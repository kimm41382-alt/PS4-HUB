'use strict'; var test = require('node:test'), assert = require('node:assert/strict'), helpers = require('./helpers'), detector = require('../src/rpi/rpi-detector'); test('detects the dedicated 12801 adapter', async function () { helpers.mockXhr({ 'http://192.168.1.10:12801/api/status': { body: { ok: true } } }); var result = await helpers.detect(detector, '192.168.1.10'); assert.equal(result.target.port, 12801); assert.equal(result.target.adapter.name, 'RPI 12801'); });
