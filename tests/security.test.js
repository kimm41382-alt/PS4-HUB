'use strict'; var test = require('node:test'), assert = require('node:assert/strict'), normalizer = require('../src/games/games-normalizer'), validator = require('../src/shared/package-validator'); test('sanitizes display strings and blocks script package URLs', function () { assert.equal(normalizer.normalizeGame({ title: '<img>' }, 0).title, 'img'); assert.equal(validator.validatePackageUrl('javascript:x').valid, false); });
