'use strict'; var test = require('node:test'), assert = require('node:assert/strict'), normalizer = require('../src/games/games-normalizer'); test('normalizes aliases and preserves missing-data games', function () { var games = normalizer.normalizeGames([{ name: ' A <Game>', pkg: 'https://x/a.PKG', thumbnail: '/a.jpg', id: 'CUSA1' }, {}]); assert.equal(games[0].title, 'A Game'); assert.equal(games[0].packageUrl, 'https://x/a.PKG'); assert.equal(games[1].title, 'Untitled game'); });
