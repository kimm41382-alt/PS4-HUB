'use strict'; var test = require('node:test'), assert = require('node:assert/strict'), helpers = require('./helpers'), detector = require('../src/rpi/rpi-detector'); test('falls back from 12801 to 12800', async function () { helpers.mockXhr({ 'http://host:12800/api/status': { body: { ok: true } } }); var result = await helpers.detect(detector, 'host'); assert.equal(result.target.port, 12800); assert.equal(result.log[0].port, 12801); });
