'use strict'; var test = require('node:test'), assert = require('node:assert/strict'), fs = require('node:fs'), path = require('node:path'); test('browser source avoids unsupported mandatory syntax', function () { var source = fs.readFileSync(path.join(__dirname, '..', 'src', 'app.js'), 'utf8'); assert.equal(source.indexOf('async ') === -1, true); assert.equal(source.indexOf('?.') === -1, true); assert.equal(source.indexOf('=>') === -1, true); });
