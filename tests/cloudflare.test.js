'use strict'; var test = require('node:test'), assert = require('node:assert/strict'), fs = require('node:fs'), path = require('node:path'); test('worker includes required CORS and security headers', function () { var source = fs.readFileSync(path.join(__dirname, '..', 'worker.js'), 'utf8'); ['Access-Control-Allow-Origin', 'Access-Control-Allow-Methods', 'Access-Control-Allow-Headers', 'X-Content-Type-Options'].forEach(function (header) { assert.ok(source.indexOf(header) !== -1); }); });
