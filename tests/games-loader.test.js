'use strict'; var test = require('node:test'), assert = require('node:assert/strict'), validator = require('../src/games/games-validator'); test('accepts games arrays and rejects malformed payloads', function () { assert.equal(validator.validatePayload([{ title: 'A' }]).valid, true); assert.equal(validator.validatePayload({ games: [] }).valid, true); assert.equal(validator.validatePayload({ nope: [] }).code, 'GAMES_FORMAT_INVALID'); });
