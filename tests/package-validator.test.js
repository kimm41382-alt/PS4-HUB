'use strict'; var test = require('node:test'), assert = require('node:assert/strict'), validator = require('../src/shared/package-validator'); test('accepts HTTP(S) pkg URLs only', function () { assert.equal(validator.validatePackageUrl('https://host/a.PKG').valid, true); assert.equal(validator.validatePackageUrl('javascript:alert(1)').code, 'PACKAGE_URL_UNSAFE'); assert.equal(validator.validatePackageUrl('https://host/a.zip').code, 'PACKAGE_URL_EXTENSION'); });
