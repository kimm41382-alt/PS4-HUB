'use strict'; var test = require('node:test'), assert = require('node:assert/strict'), resolver = require('../src/image-resolver'); test('resolves safe image URLs and blocks unsafe protocols', function () { assert.equal(resolver.resolve('javascript:alert(1)'), resolver.FALLBACK); assert.equal(resolver.resolve('//cdn.example/a.jpg'), 'https://cdn.example/a.jpg'); assert.equal(resolver.resolve('http://cdn.example/a.jpg'), 'https://cdn.example/a.jpg'); assert.equal(resolver.resolve('/cover.jpg'), '/cover.jpg'); });
