'use strict'; function mockXhr(routes) { global.XMLHttpRequest = function () { this.headers = {}; }; global.XMLHttpRequest.prototype.open = function (method, url) { this.method = method; this.url = url; }; global.XMLHttpRequest.prototype.setRequestHeader = function (key, value) { this.headers[key] = value; }; global.XMLHttpRequest.prototype.send = function (body) { var self = this, route = routes[this.url]; this.body = body; process.nextTick(function () { if (!route || route.error) { if (route && route.timeout) { self.ontimeout(); } else { self.onerror(); } return; } self.status = route.status || 200; self.responseText = route.body === undefined ? '{}' : JSON.stringify(route.body); self.readyState = 4; self.onreadystatechange(); }); }; global.XMLHttpRequest.prototype.abort = function () {}; } function detect(detector, host, options) { return new Promise(function (resolve) { detector.detectRPI(host, options || {}, function (error, target, log) { resolve({ error: error, target: target, log: log }); }); }); } module.exports = { mockXhr: mockXhr, detect: detect };
