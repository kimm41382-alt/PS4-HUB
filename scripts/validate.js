'use strict'; var fs = require('fs'), path = require('path'), cp = require('child_process'), root = path.resolve(__dirname, '..'); ['games.json', 'package.json'].forEach(function (file) { JSON.parse(fs.readFileSync(path.join(root, file), 'utf8')); }); ['index.html', 'styles.css', 'assets/game-placeholder.svg', 'worker.js'].forEach(function (file) { if (!fs.existsSync(path.join(root, file))) { throw new Error('Missing required file: ' + file); } }); cp.execFileSync(process.execPath, [path.join(root, 'scripts/lint.js')], { stdio: 'inherit' }); if (!/src="src\/app\.js"/.test(fs.readFileSync(path.join(root, 'index.html'), 'utf8'))) { throw new Error('HTML entry point does not include application script.'); } console.log('JSON, required assets, HTML entry point, and JavaScript validated.');
