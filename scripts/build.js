'use strict'; var fs = require('fs'), path = require('path'); var root = path.resolve(__dirname, '..'), out = path.join(root, 'dist'); function copy(source, target) { var stat = fs.statSync(source), name; if (stat.isDirectory()) { fs.mkdirSync(target, { recursive: true }); fs.readdirSync(source).forEach(function (item) { copy(path.join(source, item), path.join(target, item)); }); } else { fs.mkdirSync(path.dirname(target), { recursive: true }); fs.copyFileSync(source, target); } } fs.rmSync(out, { recursive: true, force: true }); ['index.html', 'styles.css', 'games.json', 'assets', 'src'].forEach(function (name) { copy(path.join(root, name), path.join(out, name)); }); console.log('Built dist/ with static PS4 Game Hub assets.');
