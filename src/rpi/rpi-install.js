(function (root, factory) { var api = factory(root.PS4HubPackageValidator, root.PS4HubRpiClient, root.PS4HubRpiErrors); if (typeof module === 'object' && module.exports) { module.exports = factory(require('../shared/package-validator'), require('./rpi-client'), require('./rpi-errors')); } root.PS4HubRpiInstall = api; }(this, function (validator, client, errors) { 'use strict'; function install(target, packageUrl, options, callback) { var check = validator.validatePackageUrl(packageUrl), install; if (!check.valid) { callback(check); return; } install = target.adapter.install; client.request({ url: 'http://' + target.host + ':' + target.port + install.path, method: install.method, body: { url: check.value }, timeout: options.timeout || 5000, retries: options.retries || 0 }, function (error, result) { if (error) { callback(error); return; } if (result.data && (result.data.success === false || result.data.accepted === false)) { callback(errors.make('RPI_INSTALL_REJECTED', 'Installer did not accept the package URL.')); return; } callback(null, result); }); } return { install: install }; }));
